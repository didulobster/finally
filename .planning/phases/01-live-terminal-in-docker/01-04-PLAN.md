---
phase: 01-live-terminal-in-docker
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - frontend/tsconfig.json
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/next.config.ts
  - frontend/README.md
  - frontend/src/
  - frontend/vitest.config.mts
  - frontend/vitest.setup.ts
  - .gitignore
  - test/README.md
autonomous: true
gap_closure: true
gap_ids: [G-01-1]
requirements: [FND-01, DLVR-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`npm --prefix frontend run build` succeeds at HEAD, and frontend/out/index.html contains the Heatmap and AI Assistant panel titles of the D-01/D-02 grid"
    - "frontend/tsconfig.json, package.json, package-lock.json, next.config.ts, and README.md are byte-identical to 9745561^ (the last known-good Phase 1 tree): @/* maps to ./* and zustand is a dependency again (D-09)"
    - "frontend/src/, frontend/vitest.config.mts, and frontend/vitest.setup.ts no longer exist (user decision: the Phase 1 tree is canonical; frontend unit tests are deferred to v2)"
    - "01-fresh-start (health, fresh start) and 06-sse-reconnect report 3 passed against the locally served export"
    - "The root .gitignore no longer ignores frontend/lib/ (WR-02) and ignores backend/static/ again"
    - "test/README.md lists the verified local run commands again (build, then uvicorn with STATIC_DIR= and a fresh DB_PATH)"
  artifacts:
    - path: "frontend/tsconfig.json"
      provides: "Path alias @/* resolving to the canonical frontend root (app/, components/, store/)"
      contains: "\"@/*\": [\"./*\"]"
    - path: "frontend/package.json"
      provides: "Phase 1 dependency set, including zustand for store/terminal.ts"
      contains: "\"zustand\""
    - path: "frontend/package-lock.json"
      provides: "Known-good lockfile that `npm ci` in the Dockerfile installs"
    - path: ".gitignore"
      provides: "Python packaging rules anchored to backend/, plus backend/static/ ignored"
      contains: "/backend/lib/"
    - path: "test/README.md"
      provides: "Verified local full-stack run commands"
      contains: "STATIC_DIR="
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/ChatDrawer.tsx"
      via: "tsconfig @/* alias resolving to ./*"
      pattern: "@/components/ChatDrawer"
    - from: "frontend/store/terminal.ts"
      to: "frontend/package.json"
      via: "zustand dependency"
      pattern: "from \"zustand\""
    - from: "Dockerfile"
      to: "frontend/package-lock.json"
      via: "npm ci in the frontend build stage"
      pattern: "npm ci"
  prohibitions:
    - statement: "MUST NOT edit frontend/app, frontend/components, or frontend/store in this plan; the Phase 1 tree is canonical and already builds once the config is restored"
      status: open
      verification: test
    - statement: "MUST NOT add frontend unit tests or a test runner; frontend unit tests are deferred to v2 (STATE.md, REQUIREMENTS.md)"
      status: open
      verification: test
    - statement: "MUST NOT edit test/e2e, test/playwright.config.ts, test/docker-compose.test.yml, or test/package.json"
      status: open
      verification: test
---

## Phase Goal

ROADMAP Goal (verbatim): One `docker run` opens a dark trading terminal on :8000 that shows the 10 default tickers with streaming prices, $10k cash, and a connection status dot that survives a network drop.

The user accepted this goal wording in UAT Test 5, so no user story is added.

<objective>
Close the build half of UAT gap G-01-1: make HEAD build the canonical Phase 1 frontend again. The locked user decision is that the Phase 1 tree (frontend/app, frontend/components, frontend/store) is canonical, and that frontend/src/ and its vitest setup (added by commit 9745561) come out of the build.

Purpose: `scripts/start_mac.sh --build` and the Dockerfile's `npm run build` currently fail with 6 Turbopack errors. Commit 9745561 repointed the tsconfig alias to src/ and removed zustand, which store/terminal.ts still imports. Until HEAD builds, the stale `finally` image cannot be replaced (plan 01-06 does the rebuild).
Output: the frontend config restored to the known-good tree, frontend/src/ and the vitest files removed, a root .gitignore that no longer hides frontend/lib/ (WR-02), and the verified local-run docs restored in test/README.md.
</objective>

<execution_context>
@/Users/wilsonsmacmini/Documents/Code/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/wilsonsmacmini/Documents/Code/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-terminal-in-docker/01-UAT.md
@.planning/debug/g-01-1-only-watchlist-shows.md
@.planning/phases/01-live-terminal-in-docker/01-REVIEW.md

<interfaces>
Facts verified while planning (git at d92088b):
- `git diff --stat 56aa0d1 9745561^ -- frontend test .gitignore Dockerfile scripts` is empty. So 9745561^ (= 7cfd239) has exactly the frontend that built the grid into the post-grid test-finally image.
- Commit 9745561 changed these non-src files: frontend/tsconfig.json (the @/* alias target), frontend/package.json and package-lock.json (dropped zustand; added lightweight-charts, vitest, testing-library, jsdom, @vitejs/plugin-react; bumped @types/node), frontend/next.config.ts (dropped the docstring; added images.unoptimized), frontend/README.md (now describes the src/ layout), test/README.md (removed the verified Local commands), and .gitignore (removed the `backend/static/` line).
- Commit 9745561 added these files: frontend/src/** (app, components, hooks, __tests__), frontend/vitest.config.mts, frontend/vitest.setup.ts.
- Root .gitignore line 17 is the Python-template `lib/`, unanchored. `git check-ignore -v frontend/lib/x.ts` prints `.gitignore:17:lib/`. The same template has unanchored `downloads/`, `lib64/`, `parts/`, and `var/`.
- Canonical imports that must resolve: frontend/app/page.tsx imports @/components/ChatDrawer, @/components/Header, @/components/Panel, @/components/Watchlist, @/store/terminal. frontend/store/terminal.ts imports `create` from zustand.
- zustand is in the Phase 1 Package Legitimacy Audit (01-RESEARCH.md, Approved). Restoring the lockfile installs no package that was not already audited.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end canonical build: restore the Phase 1 frontend config, remove frontend/src and vitest, then build, serve, and pass 01 + 06 locally</name>
  <reversibility rating="reversible">Deleting frontend/src is recoverable from git history (for example `git show 9745561:frontend/src/components/Terminal.tsx`). The user decided the Phase 1 tree is canonical.</reversibility>
  <precondition>The working tree is at or after d92088b, and frontend/src exists (`test -d frontend/src`). The registry is reachable for `npm --prefix frontend ci`.</precondition>
  <files>frontend/tsconfig.json, frontend/package.json, frontend/package-lock.json, frontend/next.config.ts, frontend/README.md, frontend/src/, frontend/vitest.config.mts, frontend/vitest.setup.ts</files>
  <read_first>
    - .planning/debug/g-01-1-only-watchlist-shows.md (Resolution and the 01:50Z evidence: the six build errors)
    - frontend/tsconfig.json and frontend/package.json (current, broken)
    - frontend/app/page.tsx and frontend/store/terminal.ts (the imports that must resolve; read only, do not edit)
  </read_first>
  <action>
Implement the locked user decision (Phase 1 tree canonical; frontend/src and its vitest setup out of the build) with no hand edits to config files. Restore the exact known-good files instead.

1. Restore the five config and doc files from the last known-good commit: `git checkout 9745561^ -- frontend/tsconfig.json frontend/package.json frontend/package-lock.json frontend/next.config.ts frontend/README.md`.
   - This maps the @/* alias back to the frontend root.
   - It brings zustand back (D-09: one Zustand store).
   - It returns the lockfile to the one that built the grid in Docker (D-11: the committed package-lock.json is what `npm ci` installs).
   - The dependencies 9745561 added only for the removed tree (lightweight-charts and the unit-test toolchain) drop out with it. Phase 3 adds a chart library when it builds the chart.
2. Remove the second app tree and its test setup:
   - `git rm -r -q frontend/src frontend/vitest.config.mts frontend/vitest.setup.ts`
   - then `rm -rf frontend/src` to clear any untracked or ignored leftovers.
   Frontend unit tests are deferred to v2 (STATE.md). Add no replacement test runner.
3. Reinstall exactly the lockfile: `npm --prefix frontend ci`.
4. Build and lint. Then serve the export with the real backend on a fresh DB, and run the Phase 1 binding subset through local Playwright. Both commands are in the verify block, and the second one is the 01-03 command reused verbatim. Install test/node_modules first if it is missing (`npm --prefix test ci`).
5. Do not touch frontend/app, frontend/components, or frontend/store. The D-01/D-02 grid from 57b1ed1 is already correct; only the config around it broke.

Run the npm build, uvicorn, and Playwright commands with the sandbox disabled: the Turbopack PostCSS worker and uvicorn bind local ports, as they did in 01-03.

Commit: `fix(01-04): restore the canonical Phase 1 frontend build and remove frontend/src`.
  </action>
  <verify>
    <automated>git diff --quiet 9745561^ -- frontend/tsconfig.json frontend/package.json frontend/package-lock.json frontend/next.config.ts frontend/README.md && test ! -e frontend/src && test ! -e frontend/vitest.config.mts && test ! -e frontend/vitest.setup.ts && npm --prefix frontend run build && npm --prefix frontend run lint && grep -q 'Heatmap' frontend/out/index.html && grep -q 'AI Assistant' frontend/out/index.html && echo build-ok</automated>
    <fails_when>non-zero exit, no "build-ok" line, a TypeScript/Turbopack/ESLint error in the output, or the exported index.html lacks the grid panel titles</fails_when>
    <automated>rm -f "$TMPDIR/p1-e2e.db"; DB_PATH="$TMPDIR/p1-e2e.db" STATIC_DIR="$PWD/frontend/out" LLM_MOCK=true MASSIVE_API_KEY= uv run --directory backend uvicorn app.main:app --port 8010 > "$TMPDIR/p1-uvicorn.log" 2>&1 & for i in $(seq 40); do curl -fsS localhost:8010/api/health >/dev/null 2>&1 && break; sleep 1; done; (cd test && BASE_URL=http://localhost:8010 npx playwright test e2e/01-fresh-start.spec.ts e2e/06-sse-reconnect.spec.ts --grep-invert "clicking a ticker"); RC=$?; pkill -f "uvicorn app.main:app --port 8010"; exit $RC</automated>
    <fails_when>non-zero exit, or the Playwright summary lacks "3 passed" or reports any "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet 9745561^ -- frontend/tsconfig.json frontend/package.json frontend/package-lock.json frontend/next.config.ts frontend/README.md` exits 0.
    - `frontend/tsconfig.json` contains `"@/*": ["./*"]`, and `frontend/package.json` contains `"zustand"`.
    - `git ls-files frontend/src frontend/vitest.config.mts frontend/vitest.setup.ts` prints nothing, and `test -e frontend/src` fails.
    - `npm --prefix frontend run build` exits 0, and `frontend/out/index.html` contains `Heatmap` and `AI Assistant`.
    - The local Playwright run reports `3 passed`.
    - `git diff --quiet d92088b -- frontend/app frontend/components frontend/store` exits 0, so the canonical tree is untouched.
  </acceptance_criteria>
  <done>HEAD builds again. The exported page contains the full D-01 grid, and the Phase 1 binding specs pass against it locally. Only one app tree exists, so `docker build` and `scripts/start_mac.sh --build` are no longer blocked by the frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor the Python-template ignore rules (WR-02), re-ignore backend/static, and restore the verified local-run docs</name>
  <files>.gitignore, test/README.md</files>
  <read_first>
    - .gitignore (the "Distribution / packaging" block and the "FinAlly runtime database" block at the end)
    - .planning/phases/01-live-terminal-in-docker/01-REVIEW.md (WR-02)
    - test/README.md (current "Local" section)
  </read_first>
  <action>
1. In the root .gitignore "Distribution / packaging" block, apply WR-02. Replace the five unanchored Python-template directory rules (downloads, lib, lib64, parts, var) with the same names anchored under the backend: `/backend/downloads/`, `/backend/lib/`, `/backend/lib64/`, `/backend/parts/`, `/backend/var/`.
   - Leave every other rule as is.
   - This stops the root file from silently hiding a future frontend/lib/ helper directory. Such a directory would build locally and then break a clean Docker build.
2. At the end of .gitignore, next to the FinAlly runtime database rules, re-add the `backend/static/` rule that 9745561 removed. The Docker image puts the frontend export there, and a local copy must never be committed.
3. Restore test/README.md from the last known-good commit: `git checkout 9745561^ -- test/README.md`.
   - This brings back the Local section verified in 01-03: `npm --prefix frontend run build`, then uvicorn with DB_PATH, STATIC_DIR, LLM_MOCK, and MASSIVE_API_KEY, then the Playwright steps.
   - test/README.md is documentation, not one of the binding test files.

Commit: `chore(01-04): anchor Python ignore rules to backend and restore local E2E docs`.
  </action>
  <verify>
    <automated>! git check-ignore -q frontend/lib/x.ts && ! git check-ignore -q frontend/var/x.ts && git check-ignore -q backend/lib/x.py && git check-ignore -q backend/static/index.html && git check-ignore -q db/finally.db && git diff --quiet 9745561^ -- test/README.md && grep -q 'STATIC_DIR=' test/README.md && git diff --quiet d92088b -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json && echo hygiene-ok</automated>
    <fails_when>non-zero exit or no "hygiene-ok" line: frontend/lib is still ignored, a backend rule stopped matching, backend/static is not ignored, the README differs from 9745561^, or a binding test file changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `/backend/lib/`, `/backend/lib64/`, `/backend/var/`, `/backend/parts/`, `/backend/downloads/`, and `backend/static/`.
    - `git check-ignore -q frontend/lib/x.ts` exits 1 (not ignored), and `git check-ignore -q backend/lib/x.py` exits 0.
    - `test/README.md` is identical to 9745561^ and contains `npm --prefix frontend run build` and `STATIC_DIR=`.
    - `git diff --quiet d92088b -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json` exits 0.
  </acceptance_criteria>
  <done>New frontend helper directories are tracked by git. The backend's copied static export stays out of commits. The README again tells a developer exactly how to build, serve, and test locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | `npm ci` installs the packages pinned by frontend/package-lock.json |
| repo -> Docker build context | Whatever git tracks (or fails to track) is what a clean Docker build sees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering (supply chain) | frontend/package-lock.json | medium | mitigate | The lockfile is restored byte-for-byte from 9745561^. It is the lockfile already built in Docker for Phase 1, and every direct dependency is in the 01-RESEARCH.md Package Legitimacy Audit (zustand Approved). No new package is added. |
| T-01-18 | Tampering (silent drop of source) | root .gitignore | medium | mitigate | Python-template directory rules are anchored to /backend/ (WR-02). The verify step proves frontend/lib is not ignored and the backend rules still match. |
| T-01-19 | Information disclosure | backend/static build output | low | mitigate | `backend/static/` is ignored again, so a local copy of the export (a build artifact) cannot be committed by accident. |
| T-01-20 | Tampering (test integrity) | binding E2E files | high | mitigate | `git diff --quiet d92088b -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json` must exit 0. |
</threat_model>

<verification>
- The frontend config is byte-identical to 9745561^; frontend/src and the vitest files are gone; the build and lint pass; and out/index.html contains the grid panel titles.
- Local 01-fresh-start + 06-sse-reconnect reports 3 passed against the restored export.
- The .gitignore hygiene gate prints hygiene-ok, and the binding test files are unchanged.
</verification>

<success_criteria>
- G-01-1 build blocker removed: HEAD builds the D-01 grid (FND-01), so the Docker image can be rebuilt (DLVR-01, proven in 01-06).
- The user decision is honored: one canonical app tree, no frontend unit-test toolchain in v1.
- WR-02 is fixed.
</success_criteria>

<output>
Create `.planning/phases/01-live-terminal-in-docker/01-04-SUMMARY.md` when done
</output>
