---
phase: 01-live-terminal-in-docker
plan: 06
type: execute
wave: 6
depends_on: ["01-04", "01-05"]
files_modified: []
autonomous: true
gap_closure: true
gap_ids: [G-01-1, G-01-2]
requirements: [DLVR-01, WTCH-01, HDR-01, HDR-04, BACK-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docker build -t finally .` succeeds on a clean HEAD, so the user's local `finally` tag holds the current code and `scripts/start_mac.sh` (even without --build) serves the grid"
    - "A throwaway container from the rebuilt `finally` tag answers /api/health, and its / HTML contains the Heatmap and AI Assistant panel titles of the D-01/D-02 grid"
    - "test/sse-502-probe.mjs prints PASS with maxLive=1 against a container from the rebuilt `finally` tag"
    - "01-fresh-start (health, fresh start) and 06-sse-reconnect report 3 passed on the test/docker-compose.test.yml path, and backend pytest is green"
    - "Binding test files are unchanged since d92088b, the image's static bundle has no API key names or key values, and the gate leaves the user's `finally` container and `finally-data` volume untouched"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build (npm ci + next build, then uv sync, then static copy) that now succeeds at HEAD"
      contains: "COPY --from=frontend /frontend/out ./static"
    - path: "test/sse-502-probe.mjs"
      provides: "The 502 recovery probe from 01-05, rerun here against the container"
      contains: "sse-502-probe"
  key_links:
    - from: "scripts/start_mac.sh"
      to: "Dockerfile"
      via: "runs the local image tag `finally`, which this plan rebuilds from HEAD"
      pattern: "IMAGE=finally"
    - from: "test/docker-compose.test.yml"
      to: "Dockerfile"
      via: "the finally service builds the repo image (context ..)"
      pattern: "context: \\.\\."
  prohibitions:
    - statement: "MUST NOT stop, remove, or restart the user's `finally` container, and MUST NOT remove the `finally-data` volume or run teardown against the root docker-compose.yml; only the throwaway check container and the test compose project are torn down"
      status: open
      verification: test
    - statement: "MUST NOT edit, skip, or loosen test/e2e specs, helpers.ts, playwright.config.ts, docker-compose.test.yml, or test/package.json to make the gate pass"
      status: open
      verification: test
    - statement: "MUST NOT change product code in this plan; a gate failure is a bug to root-cause and report, not to patch inside the gate"
      status: open
      verification: test
---

## Phase Goal

ROADMAP Goal (verbatim): One `docker run` opens a dark trading terminal on :8000 that shows the 10 default tickers with streaming prices, $10k cash, and a connection status dot that survives a network drop.

The user accepted this goal wording in UAT Test 5, so no user story is added.

<objective>
Re-prove Phase 1 in Docker at HEAD, after the G-01-1 and G-01-2 fixes, and refresh the user's image.

Purpose: G-01-1's direct cause was a stale local `finally` image, built before the grid commit. `scripts/start_mac.sh` reuses that tag whenever it exists. This plan does three things:
- Rebuilds the `finally` tag from HEAD. Per PLAN.md §11 the script only builds when the image is missing or `--build` is passed, so the tag itself must be refreshed.
- Proves the rebuilt image serves the D-01 grid and recovers from a 502 (G-01-2).
- Reruns the binding Phase 1 specs in the compose harness.
Output: a rebuilt `finally` image and the gate evidence in the SUMMARY. No repository files change.
</objective>

<execution_context>
@/Users/wilsonsmacmini/Documents/Code/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/wilsonsmacmini/Documents/Code/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-terminal-in-docker/01-UAT.md
@.planning/phases/01-live-terminal-in-docker/01-03-SUMMARY.md
@.planning/phases/01-live-terminal-in-docker/01-04-SUMMARY.md
@.planning/phases/01-live-terminal-in-docker/01-05-SUMMARY.md

<interfaces>
- Dockerfile stage 1 runs `npm ci` then `npm run build` in node:24-slim. Stage 3 (`app`, the default target) copies /frontend/out to /app/backend/static. The image sets STATIC_DIR=/app/backend/static and DB_PATH=/app/db/finally.db.
- scripts/start_mac.sh: `IMAGE=finally`, `CONTAINER=finally`. It builds only when `--build` is passed or the image is missing, then replaces the container, runs with `-v finally-data:/app/db`, and uses `--env-file .env`.
- The compose gate command that passed in 01-03 is reused verbatim (verify block). The Phase 1 subset excludes "clicking a ticker", which belongs to Phase 3.
- test/sse-502-probe.mjs (from 01-05): `node test/sse-502-probe.mjs <app-url>` prints `sse-502-probe: PASS ...` and exits 0, or prints `FAIL stage=...` and exits 1. It needs test/node_modules and Playwright chromium.
- Current user state, per the G-01-1 debug session: container `finally` runs the stale image a8ecec43 with the `finally-data` volume. This plan rebuilds the tag only. The user restarts the container with `scripts/start_mac.sh` during the human check.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end in the real image: rebuild the `finally` tag at HEAD, then prove a container from it serves the grid and recovers from a 502</name>
  <precondition>Plans 01-04 and 01-05 are merged: `test ! -e frontend/src` and `grep -q REOPEN_DELAY_MS frontend/store/terminal.ts` both succeed. `docker info` succeeds with the sandbox disabled. `npm --prefix test ci` has run in this worktree, and Playwright chromium is installed.</precondition>
  <files>No repository files. This task rebuilds the local Docker image tag `finally`.</files>
  <read_first>
    - Dockerfile and .dockerignore
    - scripts/start_mac.sh (which tag and container names are the user's)
    - test/sse-502-probe.mjs (usage line and exit codes)
  </read_first>
  <action>
1. Record the "before" state for the SUMMARY:
   - `docker image inspect finally --format '{{.Id}} {{.Created}}'`
   - `docker ps -a --filter name=^finally$ --format '{{.ID}} {{.Image}} {{.Status}}'`
   - `docker volume ls --format '{{.Name}}' | grep -x finally-data`
2. Run the verify command. It:
   - checks that frontend, backend, Dockerfile, and .dockerignore have no uncommitted changes, so the image is exactly HEAD;
   - runs `docker build -t finally .`, which refreshes the user's tag;
   - starts a throwaway container `finally-gap-check` from that tag on host port 8011 with LLM_MOCK=true, an empty MASSIVE_API_KEY, and no volume, so the DB is fresh and the user's volume is untouched;
   - waits for /api/health;
   - saves / to $TMPDIR/g01-index.html and checks it contains the Heatmap and AI Assistant titles;
   - runs the 502 probe against http://127.0.0.1:8011;
   - always removes `finally-gap-check`.
3. Record the "after" state with the same three commands. The image Id and Created must be new. The user's container line and the volume line must be unchanged.
4. If the build or a check fails, root-cause it and report it in the SUMMARY with the failing output. Do not patch product code in this plan and do not loosen any check.

Run all docker, curl, and probe commands with the sandbox disabled (docker socket, local ports).
  </action>
  <verify>
    <automated>[ -z "$(git status --porcelain -- frontend backend Dockerfile .dockerignore)" ] && docker build -t finally . && docker run -d --name finally-gap-check -p 8011:8000 -e LLM_MOCK=true -e MASSIVE_API_KEY= finally && (for i in $(seq 40); do curl -fsS localhost:8011/api/health >/dev/null 2>&1 && break; sleep 1; done; curl -fsS localhost:8011/ > "$TMPDIR/g01-index.html" && grep -q 'Heatmap' "$TMPDIR/g01-index.html" && grep -q 'AI Assistant' "$TMPDIR/g01-index.html" && node test/sse-502-probe.mjs http://127.0.0.1:8011); RC=$?; docker rm -f finally-gap-check >/dev/null 2>&1; exit $RC</automated>
    <fails_when>non-zero exit: uncommitted changes in the build inputs, a docker build error, the served HTML lacks a grid panel title, or the probe prints FAIL or anything other than PASS with maxLive=1</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the `finally` image Id and Created before and after. The after Created is later than the 01-05 fix commit time.
    - `$TMPDIR/g01-index.html` contains `Heatmap` and `AI Assistant`.
    - The probe line against port 8011 contains `PASS` and `maxLive=1`.
    - `[ -z "$(docker ps -aq --filter name=^finally-gap-check$)" ]` succeeds after the task.
    - The user's `finally` container line (ID, image, status) and the `finally-data` volume line are identical before and after.
  </acceptance_criteria>
  <done>The user's `finally` image is the current HEAD. A container from it serves the full D-01 grid and recovers from a 502 reconnect without a reload. The user's running container and data volume were not touched.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 1 gate at HEAD: binding specs on the compose path, pytest, integrity and shipped-bundle gates</name>
  <precondition>Task 1 passed. `docker info` succeeds with the sandbox disabled.</precondition>
  <files>No repository files. This task is verification only.</files>
  <read_first>
    - test/docker-compose.test.yml (the finally service on a tmpfs DB; the playwright service runs npm ci, then the given command)
    - .planning/phases/01-live-terminal-in-docker/01-03-SUMMARY.md (the D3 and D4 evidence to match)
  </read_first>
  <action>
1. Run the backend pytest suite (UV_CACHE_DIR=$TMPDIR/uvcache), using the verify command.
2. Run the compose gate, reused verbatim from 01-03:
   - build the test compose `finally` service;
   - run the playwright service with 01-fresh-start and 06-sse-reconnect, excluding "clicking a ticker";
   - always finish with `docker compose -f test/docker-compose.test.yml down -v`, which drops only the test project's containers and volume.
   Quote the Playwright summary line in the SUMMARY. A real test failure is a bug to root-cause and report, not a reason to change the harness.
3. Run the integrity and shipped-bundle gate:
   - binding test files unchanged since d92088b;
   - only relative URLs in frontend source;
   - no API key names or key-shaped values anywhere under /app/backend/static inside the rebuilt `finally` image. Check this through a one-off `docker run --rm` with a shell entrypoint.
4. Confirm again that the user's `finally` container line and the `finally-data` volume are unchanged since Task 1's "before" record, and that the test compose project has no containers left.

Run docker and pytest with the sandbox disabled.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=$TMPDIR/uvcache uv run --directory backend pytest -q</automated>
    <fails_when>non-zero exit, or the summary contains "failed", "error", or "skipped"</fails_when>
    <automated>docker compose -f test/docker-compose.test.yml build finally && docker compose -f test/docker-compose.test.yml run --rm playwright sh -c "npm ci && npx playwright test e2e/01-fresh-start.spec.ts e2e/06-sse-reconnect.spec.ts --grep-invert 'clicking a ticker'"; RC=$?; docker compose -f test/docker-compose.test.yml down -v; exit $RC</automated>
    <fails_when>non-zero exit, or the Playwright summary lacks "3 passed" or reports any "failed"</fails_when>
    <automated>git diff --quiet d92088b -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json && ! grep -rnE 'https?://' frontend/app frontend/components frontend/store && docker run --rm --entrypoint sh finally -c "! grep -rlE 'OPENROUTER|sk-or-' /app/backend/static" && echo gate-ok</automated>
    <fails_when>non-zero exit or no "gate-ok" line: a binding test file changed, an absolute URL is in frontend source, or a key name or value is in the shipped bundle</fails_when>
    <human-check>Run `scripts/start_mac.sh` from the repo root. No `--build` is needed, because this plan rebuilt the `finally` tag at HEAD. The script replaces the old container and keeps the finally-data volume. Open http://localhost:8000 in a window of about 1600x1000 and check:
1. The full D-01 grid shows: header (FinAlly, Total value, Cash, LIVE dot) on top; Watchlist on the left; Chart over Trade in the center; the AI Assistant drawer open on the right; Heatmap, P&L, and Positions along the bottom (UAT Test 1).
2. The theme is dark with no pure black, and the numbers are monospace.
3. The drawer toggle collapses the drawer to a thin edge tab and expands it again.</human-check>
  </verify>
  <acceptance_criteria>
    - pytest reports at least 75 passed, with 0 failed, 0 errors, and 0 skipped.
    - The compose run reports `3 passed`, and the SUMMARY quotes the line.
    - `docker compose -f test/docker-compose.test.yml ps -a -q` prints nothing after the gate.
    - The gate command prints `gate-ok`.
    - The user's `finally` container line and the `finally-data` volume line match Task 1's "before" record.
  </acceptance_criteria>
  <done>ROADMAP Phase 1 SC1 to SC5 are re-proven at HEAD in Docker after the gap fixes: the image builds and serves the grid on port 8000; fresh start and reconnect pass in the container; the 502 path recovers; pytest is green; and the binding tests are unchanged. The user's own start script now serves the current terminal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate -> user's Docker host | The gate builds an image tag the user runs, and it starts and removes containers on the user's engine |
| image bundle -> browser | /app/backend/static is served to any client on port 8000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Tampering / data loss | user's `finally` container and `finally-data` volume | high | mitigate | The gate only creates and removes `finally-gap-check` (no volume) and tears down only the test compose project (`down -v` on test/docker-compose.test.yml). The before and after records prove the user's container and volume are unchanged. |
| T-01-27 | Tampering (image provenance) | `finally` tag rebuild | medium | mitigate | The build runs only when frontend, backend, Dockerfile, and .dockerignore have no uncommitted changes, so the tag is exactly HEAD. The Id and Created are recorded before and after. |
| T-01-28 | Information disclosure | /app/backend/static in the image | high | mitigate | The gate greps the shipped bundle inside the rebuilt image for key names and key-shaped values and fails on any match. |
| T-01-29 | Tampering (test integrity) | binding E2E files | high | mitigate | `git diff --quiet d92088b -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json` must exit 0. |
</threat_model>

<verification>
- `docker build -t finally .` succeeds on a clean HEAD. A throwaway container serves the grid titles and passes the 502 probe.
- The compose path reports 3 passed for 01-fresh-start + 06-sse-reconnect, and pytest is green.
- gate-ok is printed. The user's container and volume are unchanged, and no test compose containers remain.
</verification>

<success_criteria>
- G-01-1 closed: the `finally` tag is current, so `scripts/start_mac.sh` serves the full D-01 grid (DLVR-01, WTCH-01, HDR-01).
- G-01-2 closed in the shipped image: the stream recovers from a non-200 reconnect (HDR-04).
- ROADMAP Phase 1 SC1 to SC5 re-proven at HEAD, and pytest green (BACK-02).
</success_criteria>

<output>
Create `.planning/phases/01-live-terminal-in-docker/01-06-SUMMARY.md` when done
</output>
