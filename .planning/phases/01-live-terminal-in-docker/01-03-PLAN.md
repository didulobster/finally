---
phase: 01-live-terminal-in-docker
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/components/Panel.tsx
  - frontend/components/ChatDrawer.tsx
  - frontend/app/page.tsx
  - test/README.md
autonomous: true
requirements: [FND-01, FND-02, HDR-01, HDR-03, HDR-04, WTCH-01, DLVR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "At md width and above the page is laid out per D-01: header on top; Watchlist panel on the left; Chart and Trade panels stacked in the center; the AI Assistant drawer on the right; and a bottom strip of Heatmap, P&L, and Positions spanning the full width"
    - "At a 1600x1000 viewport the terminal fills the window with no page scroll: a full-page screenshot is exactly 1000 px tall, and the watchlist scrolls inside its panel"
    - "At a 390 px wide viewport the panels stack into one scrolling column: a full-page screenshot is taller than 844 px"
    - "Panels not built yet (Chart, Trade, Heatmap, P&L, Positions, AI Assistant) show their title and muted text naming the phase that fills them, and carry no Phase 2-4 testids"
    - "The AI Assistant drawer is open on page load, and its toggle collapses it to a thin edge tab and expands it again without a reload"
    - "01-fresh-start (health, fresh start) and 06-sse-reconnect report 3 passed against the container built by test/docker-compose.test.yml, on a fresh tmpfs DB"
    - "At phase end the backend pytest suite passes, and test/e2e, playwright.config.ts, docker-compose.test.yml, and test/package.json are identical to main"
  artifacts:
    - path: "frontend/components/Panel.tsx"
      provides: "Titled terminal panel with an internally scrolling body, plus a muted note body for panels not built yet"
      exports: ["Panel", "PanelNote"]
    - path: "frontend/components/ChatDrawer.tsx"
      provides: "Right-hand AI Assistant drawer, open by default, collapsible to an edge tab"
      exports: ["ChatDrawer"]
    - path: "frontend/app/page.tsx"
      provides: "D-01 terminal grid composing Header, Watchlist, Panel, and ChatDrawer; still the only caller of connect()"
      contains: "useEffect(connect, [])"
    - path: "test/README.md"
      provides: "Verified local full-stack E2E commands (build the export, serve it with uvicorn on a fresh DB)"
      contains: "STATIC_DIR="
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/Panel.tsx"
      via: "each region rendered as <Panel title=...>"
      pattern: "<Panel title="
    - from: "frontend/app/page.tsx"
      to: "frontend/components/ChatDrawer.tsx"
      via: "right column of the upper area"
      pattern: "<ChatDrawer"
    - from: "frontend/app/page.tsx"
      to: "frontend/components/Watchlist.tsx"
      via: "Watchlist rendered inside the left Panel"
      pattern: "<Watchlist"
    - from: "test/docker-compose.test.yml"
      to: "Dockerfile"
      via: "finally service builds the repo image (context ..) that serves frontend/out"
      pattern: "context: \\.\\."
  prohibitions:
    - statement: "MUST NOT fill panels that are not built yet with invented sample data (fake positions, fake chart lines, fake chat messages, fake trade results); they show only a title and a note naming the phase that builds them"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit, skip, or loosen test/e2e specs, helpers.ts, playwright.config.ts, or docker-compose.test.yml to make the phase gate pass"
      status: resolved
      verification: test
    - statement: "MUST NOT run docker compose down -v, docker volume rm, or docker rm against the user's root docker-compose.yml project, the finally container, or the finally-data volume; teardown targets only the test compose project and the phase's own check containers"
      status: resolved
      verification: test
---

## Phase Goal

ROADMAP Goal (verbatim): One `docker run` opens a dark trading terminal on :8000 that shows the 10 default tickers with streaming prices, $10k cash, and a connection status dot that survives a network drop.

Note: this Goal is not in user-story format. The planner did not invent a story. Run `/gsd-mvp-phase 1` to set one before `/gsd-verify-work`.

<objective>
Lay out the full terminal grid around the working skeleton (D-01 to D-04), so later phases fill their panels without layout shifts. Then run the phase gate (D-15 step 4): the binding E2E specs against the real container, pytest, and the integrity and URL gates.

Purpose: complete the ROADMAP Phase 1 success criteria. SC1 (image builds and serves), SC2 and SC3 (fresh-start view and reconnect, re-proven in the container), SC4 (pytest green), and SC5 (01-fresh-start health and fresh-start tests plus 06-sse-reconnect pass against the container).
Output: `frontend/components/Panel.tsx`, `frontend/components/ChatDrawer.tsx`, the grid in `frontend/app/page.tsx`, and a verified local-run section in `test/README.md`.
</objective>

<execution_context>
@/Users/wilsonsmacmini/Documents/Code/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/wilsonsmacmini/Documents/Code/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-terminal-in-docker/01-CONTEXT.md
@.planning/phases/01-live-terminal-in-docker/01-RESEARCH.md
@.planning/phases/01-live-terminal-in-docker/01-PATTERNS.md
@.planning/phases/01-live-terminal-in-docker/SKELETON.md
@.planning/phases/01-live-terminal-in-docker/01-02-SUMMARY.md

<interfaces>
Created by plan 01-02 (read the files, as they are the source of truth):
- frontend/store/terminal.ts: `useTerminal` (Zustand hook; state status, prices, watchlist, cash, totalValue); `connect(): () => void`; types `Status`, `Price`, `TerminalState`.
- frontend/components/Header.tsx: `Header()`, a full-width top bar with total-value, cash-balance, and connection-status. It takes no props.
- frontend/components/Watchlist.tsx: `Watchlist()`, which renders the rows only (no panel chrome and no height constraints). It takes no props.
- frontend/app/page.tsx: "use client", `useEffect(connect, [])`, and currently renders Header plus a "Watchlist" section.
- frontend/app/globals.css @theme utilities: bg-bg, bg-panel, border-border, text-muted, text-text, text-accent, text-primary, bg-submit, text-up, text-down, bg-up, bg-down.
Binding E2E contract unchanged: `[data-testid^="watchlist-row-"]` count stays 10; Phase 1 testids are only connection-status, cash-balance, total-value, watchlist-row-{T}, watchlist-price-{T} (and the non-contract watchlist-change-{T}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 terminal grid with titled panels and a collapsible AI Assistant drawer, filling the viewport</name>
  <reversibility rating="costly">D-01 grid; every later phase slots its panel into these regions. Decided by the user in CONTEXT.md, so recorded, not gated.</reversibility>
  <precondition>frontend/components/Header.tsx, frontend/components/Watchlist.tsx, and frontend/store/terminal.ts exist from plan 01-02, and frontend/node_modules is installed (run `npm --prefix frontend ci` in a fresh worktree).</precondition>
  <files>frontend/components/Panel.tsx, frontend/components/ChatDrawer.tsx, frontend/app/page.tsx</files>
  <read_first>
    - frontend/app/page.tsx (current root page from 01-02)
    - frontend/components/Header.tsx and frontend/components/Watchlist.tsx (props and markup to compose, not to change)
    - frontend/app/globals.css (theme token names)
    - .planning/phases/01-live-terminal-in-docker/01-CONTEXT.md (D-01 to D-04 and the layout sketch in the specifics section)
    - .planning/phases/01-live-terminal-in-docker/01-PATTERNS.md (Panel, ChatDrawer, and grid section)
    - planning/PLAN.md §2 Visual Design and §10 Layout
  </read_first>
  <action>
Implement D-01 to D-04 around the existing live Header and Watchlist. Proportions, spacing, and copy are Claude's discretion, within the PLAN.md colors.

frontend/components/Panel.tsx: export `Panel({ title, children, className })`, where title is a string, children is optional ReactNode, and className is an optional string.
- It renders a section with bg-panel, a 1px border-border, and flex flex-col min-h-0.
- A 28px header row shows the title in 11px uppercase letter-spaced text-muted, with a bottom border.
- The body is flex-1 min-h-0 overflow-auto, so panels scroll internally (D-04).
- Also export `PanelNote({ children })`: muted small text with padding, used as the body of panels not built yet (D-02).
- No testids.

frontend/components/ChatDrawer.tsx ("use client"): export `ChatDrawer()`.
- Hold local `useState(true)` for open. It starts open (D-03) and stays out of the Zustand store.
- Open: an aside of about 20rem at md and up. The header row reads "AI Assistant" and has a toggle button with aria-label "Collapse chat" and aria-expanded true. The body is a PanelNote saying the assistant arrives in Phase 4.
- Collapsed: a thin edge tab about 2rem wide at full height, with a vertical "AI" label and a button with aria-label "Expand chat" and aria-expanded false.
- The toggle uses text-accent on hover.

frontend/app/page.tsx: keep "use client" and the single `useEffect(connect, [])`. Replace the body with the D-01 grid:
- The root is a flex column with bg-bg, min-h-screen on small screens and `md:h-screen md:overflow-hidden` at md and up (D-04, 100vh, no page scroll).
- `<Header />` sits on top as a shrink-0 row.
- The upper area is flex-1 min-h-0 and a row at md and up:
  - left: `<Panel title="Watchlist">` about 18rem wide, containing `<Watchlist />`;
  - center: a flex-1 min-w-0 column with `<Panel title="Chart">` (flex-1) over `<Panel title="Trade">` (about 6rem tall), each with a PanelNote naming Phase 3 (chart) and Phase 2 (trade bar);
  - right: `<ChatDrawer />`.
- The bottom strip spans the full width and is about 32% of the height at md and up. It is a 3-column grid (1fr 1fr 1.3fr) with `<Panel title="Heatmap">`, `<Panel title="P&L">`, and `<Panel title="Positions">`, each with a PanelNote naming Phase 2.
- Below md, everything stacks into one scrolling column, and each panel gets a min height of about 12rem so it stays visible (D-04).
- Use 4px gaps and no pure black.

Rules:
- Add no testids from the Phase 2-4 contracts (chart, trade bar, heatmap, P&L, positions, chat). The phases that build those panels add them together with the behavior.
- Do not show invented sample data in any panel not built yet.
- Run the verify commands with the sandbox disabled (next build, uvicorn, and Playwright bind ports).
  </action>
  <verify>
    <automated>npm --prefix frontend run build && npm --prefix frontend run lint</automated>
    <fails_when>non-zero exit, or a TypeScript or ESLint error in the output</fails_when>
    <automated>rm -f "$TMPDIR/p1-e2e.db"; DB_PATH="$TMPDIR/p1-e2e.db" STATIC_DIR="$PWD/frontend/out" LLM_MOCK=true MASSIVE_API_KEY= uv run --directory backend uvicorn app.main:app --port 8010 > "$TMPDIR/p1-uvicorn.log" 2>&1 & for i in $(seq 40); do curl -fsS localhost:8010/api/health >/dev/null 2>&1 && break; sleep 1; done; (cd test && BASE_URL=http://localhost:8010 npx playwright test e2e/01-fresh-start.spec.ts e2e/06-sse-reconnect.spec.ts --grep-invert "clicking a ticker" && npx playwright screenshot --full-page --viewport-size=1600,1000 --wait-for-timeout=3000 http://localhost:8010 "$TMPDIR/phase1-desktop.png" && npx playwright screenshot --full-page --viewport-size=390,844 --wait-for-timeout=3000 http://localhost:8010 "$TMPDIR/phase1-mobile.png"); RC=$?; pkill -f "uvicorn app.main:app --port 8010"; [ $RC -eq 0 ] && node -e 'const f=require("fs");const h=p=>f.readFileSync(p).readUInt32BE(20);const d=h(process.argv[1]),m=h(process.argv[2]);console.log("desktop",d,"mobile",m);process.exit(d===1000&&m>844?0:1)' "$TMPDIR/phase1-desktop.png" "$TMPDIR/phase1-mobile.png"</automated>
    <fails_when>non-zero exit; the Playwright summary lacks "3 passed"; or the printed desktop height is not 1000 (the page scrolls at 1600x1000) or the mobile height is 844 or less (panels did not stack)</fails_when>
    <automated>! grep -rnE 'data-testid="(main-chart|price-chart|chart-price|trade-|heatmap|pnl-chart|position|chat-)' frontend/app frontend/components && grep -q 'md:h-screen' frontend/app/page.tsx && grep -q 'aria-expanded' frontend/components/ChatDrawer.tsx && echo layout-gates-ok</automated>
    <fails_when>non-zero exit or no "layout-gates-ok" line (a later-phase testid was added, the viewport lock is missing, or the drawer toggle lacks aria-expanded)</fails_when>
    <human-check>Open $TMPDIR/phase1-desktop.png and $TMPDIR/phase1-mobile.png, then check:
1. Desktop matches the D-01 sketch: header (FinAlly, Total value, Cash, LIVE dot) on top; Watchlist left; Chart over Trade in the center; AI Assistant drawer open on the right; Heatmap, P&L, and Positions across the bottom.
2. The theme is dark (#0d1117 and #1a1a2e) with no pure black, and numbers are monospace.
3. Mobile shows one stacked column.
4. In a browser on the served app, the drawer toggle collapses the drawer to a thin edge tab and expands it again.</human-check>
  </verify>
  <acceptance_criteria>
    - `frontend/components/Panel.tsx` exports `Panel` and `PanelNote`, and contains `overflow-auto` and `min-h-0`.
    - `frontend/components/ChatDrawer.tsx` contains `useState(true)`, `AI Assistant`, `aria-expanded`, `Collapse chat`, and `Expand chat`.
    - `frontend/app/page.tsx` contains `useEffect(connect, [])`, `md:h-screen`, `md:overflow-hidden`, `<ChatDrawer`, `<Watchlist`, `<Header`, and Panel titles `Watchlist`, `Chart`, `Trade`, `Heatmap`, `P&L`, and `Positions`.
    - `grep -rnE 'data-testid="(main-chart|price-chart|chart-price|trade-|heatmap|pnl-chart|position|chat-)' frontend/app frontend/components` prints nothing.
    - Local E2E reports `3 passed`. The desktop full-page screenshot is exactly 1000 px tall, and the mobile full-page screenshot is taller than 844 px.
  </acceptance_criteria>
  <done>A user sees the full Bloomberg-style grid at desktop width with no page scroll: live watchlist left, titled Chart and Trade panels center, an open AI Assistant drawer on the right that collapses to an edge tab, and Heatmap, P&L, and Positions panels along the bottom. On a narrow screen it stacks into one column, and the Phase 1 E2E specs still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate in Docker: 01-fresh-start and 06-sse-reconnect against the container, pytest, integrity and URL gates, local-run docs</name>
  <precondition>Docker engine reachable (`docker info` succeeds with the sandbox disabled), and test/node_modules installed for the fallback path.</precondition>
  <files>test/README.md</files>
  <read_first>
    - test/docker-compose.test.yml (finally service on tmpfs /app/db, playwright service runs npm ci then npx playwright test)
    - test/README.md (the "Local" section to update)
    - test/playwright.config.ts (BASE_URL default)
    - Dockerfile
    - .planning/phases/01-live-terminal-in-docker/01-RESEARCH.md (Validation Architecture, Pitfall 6, Assumption A1)
  </read_first>
  <action>
Per D-15 step (4), prove the phase in the container.

1. Run the backend pytest suite (UV_CACHE_DIR=$TMPDIR/uvcache).

2. Run the Docker gate from the verify block:
   - build the finally service of test/docker-compose.test.yml;
   - run the playwright service with the Phase 1 subset (01-fresh-start and 06-sse-reconnect, excluding the "clicking a ticker" test, which belongs to Phase 3, per Pitfall 6);
   - always finish with `docker compose -f test/docker-compose.test.yml down -v`, so the next run gets a fresh tmpfs DB.

3. If the compose `run` path itself fails to start or wait for the finally service (research Assumption A1, a compose-level error rather than a test failure), use the fallback:
   - `docker build -t finally .`
   - `docker run -d --name finally-phase1-e2e -p 8010:8000 -e LLM_MOCK=true -e MASSIVE_API_KEY= finally`. There is no volume, so the DB is fresh.
   - Wait for /api/health on localhost:8010.
   - Run the same Playwright subset from test/ with BASE_URL pointing at localhost:8010.
   - `docker rm -f finally-phase1-e2e`.

   Record which path ran, and the Playwright summary line, in the SUMMARY. A real test failure is a bug to root-cause, not a reason to switch paths.

4. Run the integrity and bundle gates from the verify block:
   - binding test files identical to main;
   - no absolute URLs in the frontend source;
   - no API key names or key values in the built frontend/out.

5. Update test/README.md: replace the "Local" section's vague first line with the verified commands, in 6 lines or fewer:
   - `npm --prefix frontend run build`;
   - from the repo root, start the app with DB_PATH set to a fresh file, STATIC_DIR set to the absolute frontend/out path, LLM_MOCK=true, and MASSIVE_API_KEY empty, using `uv run --directory backend uvicorn app.main:app --port 8000`;
   - then the existing npm ci and npx playwright steps.

   Keep the Docker section as is.

Never run teardown against the root docker-compose.yml, the finally container, or the finally-data volume.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=$TMPDIR/uvcache uv run --directory backend pytest -q</automated>
    <fails_when>non-zero exit, or the summary contains "failed", "error", or "skipped"</fails_when>
    <automated>docker compose -f test/docker-compose.test.yml build finally && docker compose -f test/docker-compose.test.yml run --rm playwright sh -c "npm ci && npx playwright test e2e/01-fresh-start.spec.ts e2e/06-sse-reconnect.spec.ts --grep-invert 'clicking a ticker'"; RC=$?; docker compose -f test/docker-compose.test.yml down -v; exit $RC</automated>
    <fails_when>non-zero exit, or the Playwright summary lacks "3 passed" or reports any "failed"</fails_when>
    <automated>git diff --quiet main -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json && ! grep -rnE 'https?://' frontend/app frontend/components frontend/store && ! grep -rlE 'OPENROUTER|sk-or-' frontend/out && grep -q 'STATIC_DIR=' test/README.md && grep -q 'npm --prefix frontend run build' test/README.md && echo gate-ok</automated>
    <fails_when>non-zero exit or no "gate-ok" line (a binding test file differs from main, an absolute URL is in frontend source, a key name or value is in the bundle, or the README lacks the verified commands)</fails_when>
  </verify>
  <acceptance_criteria>
    - pytest reports at least 75 passed, with 0 failed, 0 errors, and 0 skipped.
    - The container E2E run (compose path, or the documented A1 fallback) reports `3 passed`, and the SUMMARY quotes the summary line and names the path used.
    - `docker compose -f test/docker-compose.test.yml ps -a` lists no containers after the gate.
    - `docker ps -a --format '{{.Names}}' | grep -x finally-phase1-e2e` prints nothing.
    - `git diff --quiet main -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json` exits 0.
    - `test/README.md` contains `npm --prefix frontend run build`, `STATIC_DIR=`, and `uv run --directory backend uvicorn app.main:app`.
    - `docker volume ls --format '{{.Name}}' | grep -x finally-data` returns the same result before and after the gate, so the user's volume is untouched.
  </acceptance_criteria>
  <done>The ROADMAP Phase 1 success criteria are proven: the image builds and serves on port 8000; 01-fresh-start (health, fresh start) and 06-sse-reconnect pass against the container; pytest is green; the binding tests are unchanged; and the local full-stack run is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test compose project -> Docker host | The gate builds, runs, and tears down containers and volumes on the user's Docker engine |
| build output -> browser | frontend/out is served to any client on port 8000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering / data loss | docker compose teardown | high | mitigate | Teardown is only `docker compose -f test/docker-compose.test.yml down -v` (test project, tmpfs DB) and `docker rm -f finally-phase1-e2e`. The acceptance check shows the finally-data volume is unchanged. |
| T-01-13 | Information disclosure | frontend/out bundle | high | mitigate | The Task 2 gate fails if any key name or key-shaped value appears in frontend/out. The frontend reads no env vars (01-02 gate). |
| T-01-14 | Tampering (XSS) | Panel and ChatDrawer | low | mitigate | Static text and React children only. The 01-02 raw-HTML gate still covers frontend/app and components. |
| T-01-15 | Tampering (test integrity) | test/e2e specs and config | high | mitigate | `git diff --quiet main -- test/e2e test/playwright.config.ts test/docker-compose.test.yml test/package.json` must exit 0. |
| T-01-16 | Spoofing (misleading UI) | panels not built yet | medium | mitigate | Only a title and a note naming the phase, with no invented data (prohibition 1), reviewed in the human check. |
</threat_model>

<verification>
- Local E2E (01 health and fresh start, 06 reconnect) reports 3 passed after the grid change. The desktop full-page screenshot is 1000 px tall and the mobile one is taller than 844 px.
- The Docker gate: `docker compose -f test/docker-compose.test.yml` build plus run of the Phase 1 subset reports 3 passed, followed by `down -v`.
- pytest is green, and the binding test files are identical to main.
</verification>

<success_criteria>
- ROADMAP Phase 1 SC1: the image builds, and the container serves the frontend and /api/health on port 8000 (the DB volume was proven in 01-02).
- SC2 and SC3: fresh start (10 rows, $10k cash and total value, live prices) and reconnect without reload, re-proven in the container.
- SC4: pytest passes (backend verified in 01-01).
- SC5: `01-fresh-start` (health and fresh-start tests) and `06-sse-reconnect` pass against the container.
- D-01 to D-04: the full terminal grid, filling the viewport, with panels for later phases and a collapsible chat drawer.
</success_criteria>

## Flagged Assumptions

- Research A1: the compose `run` gate command was not executed during research. The documented fallback (the product image run directly on host port 8010 with local Playwright) is equivalent evidence against the container.
- "Tablet width" in D-04 is taken as Tailwind's md breakpoint (768 px): the grid applies at md and up and stacks below it.

## Artifacts this phase produces

- `frontend/components/Panel.tsx`: `Panel({ title: string; children?: ReactNode; className?: string })`, `PanelNote({ children })`.
- `frontend/components/ChatDrawer.tsx`: `ChatDrawer()`, with local `open` state from `useState(true)`, and toggle buttons labeled "Collapse chat" and "Expand chat" with `aria-expanded`.
- `frontend/app/page.tsx`: the D-01 grid. Regions in order: Header; Watchlist panel, Chart panel over Trade panel, ChatDrawer; bottom strip Heatmap, P&L, Positions. Viewport lock classes `md:h-screen md:overflow-hidden`.
- `test/README.md`: "Local" section with the verified build-and-serve commands.
- Verification outputs, not committed: `$TMPDIR/phase1-desktop.png` and `$TMPDIR/phase1-mobile.png`.

<output>
Create `.planning/phases/01-live-terminal-in-docker/01-03-SUMMARY.md` when done
</output>
